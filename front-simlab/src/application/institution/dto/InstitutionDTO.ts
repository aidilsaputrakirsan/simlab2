export interface BookingTableParam {
    page: number,
    per_page: number,
    search: string,
}

export interface InstitutionTableParam {
    page: number,
    per_page: number,
    search: string,
}

export interface InstitutionInputDTO {
    name: string
}